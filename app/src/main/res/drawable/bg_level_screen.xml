<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Thin black outline -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#000000" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <!-- Yellow border (inset from black outline) -->
    <item
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFD700" />
            <corners android:radius="14dp" />
        </shape>
    </item>
    <!-- Lemon-green gradient background (darker top to lighter yellow-white bottom) -->
    <item
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="#7CB342"
                android:centerColor="#CCDB44"
                android:endColor="#848436"
                android:angle="90" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</layer-list>
